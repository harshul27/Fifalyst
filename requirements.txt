# Premier League 2026/27 - Season Stats Recorder

# Dashboard
streamlit>=1.40

# Data handling / storage (parquet)
pandas>=2.0
numpy>=1.24
pyarrow>=12.0

# HTTP (FPL API)
requests>=2.31

# Optional: FBref tactical enrichment (python record.py --fbref)
soccerdata>=1.8
